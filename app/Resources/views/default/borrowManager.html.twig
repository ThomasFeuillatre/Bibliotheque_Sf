{% extends 'base.html.twig' %}
{% block header %}
    {{ parent() }}
    <h3>Gérer les emprunts</h3>

{% endblock %}
{% block body %}
    {{ form_start(form) }}
    <div class="row">
        <div class="col s6">{{ form_row(form.book)}}</div>
        <div class="col s6">{{ form_row(form.reader)}}</div>
    </div>
    <div class="row">
        <div class="col s6">{{ form_row(form.dateBorrow)}}</div>
        <div class="col s6">{{ form_row(form.deadline)}}</div>

    </div>
    {{ form_end(form) }}
    {#{{ form_start(form) }}#}
    {#<div class="input-field col s12">#}
        {#{{ form_row(form.name) }}#}
    {#</div>#}
    {#<div class="input-field col s12">#}
        {#{{ form_row(form.firstName) }}#}
    {#</div>#}
    {#{{ form_end(form) }}#}
{% endblock %}

{% block stylesheets %}

{% endblock %}

{% block javascripts %}
    {{ parent() }}
<script>
     $(document).ready(function() {
        $('select').material_select();
        $('.datepicker').pickadate({
            selectMonths: true, // Creates a dropdown to control month
            selectYears: 7, // Creates a dropdown of 15 years to control year,
            today: 'Today',
            clear: 'Clear',
            close: 'Ok',
            format:'yyyy-mm-dd',
            closeOnSelect: true // Close upon selecting a date,
        });
    });
</script>
{% endblock %}
